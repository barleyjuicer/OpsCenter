# Private Local Data

This folder is for real local application data, including `opscenter.db`.

Do not commit real databases, personal records, PHI, PII, backups, or copied production data. `.gitignore` keeps the contents of this folder out of Git while allowing this README and `.gitkeep` to remain visible.

Use `sample_data/` for fake examples that are safe to commit.
